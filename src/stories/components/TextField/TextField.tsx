import React from "react";

/**
 * Primary UI component for user interaction
 */
export const TextField = () => {
    return (
        <input
            type="text"
            // placeholder={label}
        />
    );
};
